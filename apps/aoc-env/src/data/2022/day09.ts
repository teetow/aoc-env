import data from "aocdata/data/2022/day09";

const testData = ``;

const part1 = (data: string) => {
  return 0;
};

const part2 = (data: string) => {
  return 0;
};

const day = {
  parts: [
    {
      tests: [{ data: testData, runner: part1, result: 0 }],
      solutions: [{ data: data, runner: part1 }],
    },
    {
      // tests: [{ data: testData, runner: part2, result: 0 }],
      // solutions: [{ data: data, runner: part2 }],
    },
  ],
};

export default day;
